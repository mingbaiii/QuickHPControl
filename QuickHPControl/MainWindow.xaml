<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="QuickHPControl.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern" xmlns:ikw="http://schemas.inkore.net/lib/ui/wpf" ui:WindowHelper.UseModernWindowStyle="True" ui:WindowHelper.SystemBackdropType="Mica" ui:TitleBar.Height="48" Title="HP 性能控制" Width="540" Height="700" MinWidth="440" MinHeight="560" xmlns:ourhpcontrol="clr-namespace:QuickHPControl;assembly=QuickHPControl">
  <ui:NavigationView Name="MainNavView" IsBackButtonVisible="Collapsed" IsSettingsVisible="True" PaneDisplayMode="Top" SelectionChanged="MainNavView_SelectionChanged">
    <ui:NavigationView.MenuItems>
      <ui:NavigationViewItem Name="PerformanceItem" Content="性能控制" Tag="performance">
        <ui:NavigationViewItem.Icon>
          <ui:FontIcon Glyph="&#xE945;" FontFamily="Segoe Fluent Icons" />
        </ui:NavigationViewItem.Icon>
      </ui:NavigationViewItem>
    </ui:NavigationView.MenuItems>
    <Grid Margin="24,12,24,24">
      <Grid Name="PerformancePage" Visibility="Visible">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="性能控制" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,16" />
        <Border Grid.Row="1" Background="{DynamicResource ControlFillColorDefaultBrush}" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16,12" Margin="0,0,0,16">
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="16" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" Text="当前模式" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" />
            <TextBlock Name="TxtCurrentMode" Grid.Row="0" Grid.Column="2" FontWeight="SemiBold" VerticalAlignment="Center" Text="—" />
            <TextBlock Grid.Row="1" Grid.Column="0" Text="电源状态" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" Margin="0,8,0,0" />
            <TextBlock Name="TxtPowerStatus" Grid.Row="1" Grid.Column="2" VerticalAlignment="Center" Margin="0,8,0,0" />
            <Button Name="BtnManualRefresh" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Content="手动刷新" HorizontalAlignment="Center" Padding="16,6" Margin="0,8,0,0" Visibility="Collapsed" Click="BtnManualRefresh_Click" />
          </Grid>
        </Border>
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
          <StackPanel>
            <TextBlock Text="选择性能模式" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12" />
            <WrapPanel Name="ModePanel" />
          </StackPanel>
        </ScrollViewer>
      </Grid>
      <ui:ScrollViewerEx Name="SettingsPage" Padding="0,0,16,0" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
        <controls:SimpleStackPanel xmlns:controls="clr-namespace:iNKORE.UI.WPF.Controls;assembly=iNKORE.UI.WPF" controls:SimpleStackPanel.Spacing="4">
          <TextBlock Text="连接与状态" Style="{DynamicResource BodyStrongTextBlockStyle}" Margin="0,16,0,5" />
          <ui:SettingsCard Header="连接状态">
            <ui:SettingsCard.HeaderIcon>
              <ui:FontIcon Glyph="&#xE703;" FontFamily="Segoe Fluent Icons" />
            </ui:SettingsCard.HeaderIcon>
            <ui:SettingsCard.Description>
              <TextBlock Name="TxtConnStatusSettings" Text="未连接" />
            </ui:SettingsCard.Description>
          </ui:SettingsCard>
          <ui:SettingsCard Header="系统版本">
            <ui:SettingsCard.HeaderIcon>
              <ui:FontIcon Glyph="&#xE770;" FontFamily="Segoe Fluent Icons" />
            </ui:SettingsCard.HeaderIcon>
            <ui:SettingsCard.Description>
              <TextBlock Name="TxtVersionSettings" Text="—" />
            </ui:SettingsCard.Description>
          </ui:SettingsCard>
          <ui:SettingsCard Header="运行状态">
            <ui:SettingsCard.HeaderIcon>
              <ui:FontIcon Glyph="&#xE946;" FontFamily="Segoe Fluent Icons" />
            </ui:SettingsCard.HeaderIcon>
            <ui:SettingsCard.Description>
              <TextBlock Name="TxtRunStatusSettings" Text="就绪" />
            </ui:SettingsCard.Description>
          </ui:SettingsCard>
          <TextBlock Text="首选项" Style="{DynamicResource BodyStrongTextBlockStyle}" Margin="0,33,0,5" />
          <ui:SettingsCard Header="开机自动启动" Description="通过任务计划程序以管理员权限启动">
            <ui:SettingsCard.HeaderIcon>
              <ui:FontIcon Glyph="&#xE7E8;" FontFamily="Segoe Fluent Icons" />
            </ui:SettingsCard.HeaderIcon>
            <ui:ToggleSwitch Name="ToggleAutoStart" Toggled="ToggleAutoStart_Toggled" />
          </ui:SettingsCard>
          <TextBox Name="TxtLog" IsReadOnly="True" FontFamily="Consolas" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" MinHeight="200" />
        </controls:SimpleStackPanel>
      </ui:ScrollViewerEx>
    </Grid>
  </ui:NavigationView>
</Window>
